import { type Mnemonic, type MnemonicPassphrase } from "./Mnemonic.js";
import type { Flavor } from "../utils/Flavor.js";
export type XPub = Flavor<string, "XPub">;
export declare const maxIndex: number;
export declare const isXPub: (value: unknown) => value is XPub;
export declare const generateXPub: (mnemonic: Mnemonic, passPhrase?: MnemonicPassphrase) => XPub;
