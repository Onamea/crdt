const isUint8Array = (value) => {
    return value instanceof Uint8Array;
};
export default isUint8Array;
export const isUint8ArrayOfLength = (value, length) => {
    return isUint8Array(value) && value.length === length;
};
