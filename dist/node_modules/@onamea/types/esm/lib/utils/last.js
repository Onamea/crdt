export default (s, n = 0) => {
    if (n > s.length)
        throw new Error("Index out of range");
    const i = Math.abs(n);
    if (i === 0) {
        return s.slice(-1);
    }
    else {
        return s.slice(-i - 1, -i);
    }
};
